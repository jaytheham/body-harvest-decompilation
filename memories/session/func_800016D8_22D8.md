# func_800016D8_22D8 Attempt

## Summary
- Best score: CURRENT(1160)
- Function packs save data (5 entries of Unk80047FB8) into D_800431C0 starting at index 9
- Uses nested loops: outer loop over 5 entries, inner loop copies 6 bytes of unk0..unk5
- Then packs unk8 (4 bytes), unkC (1 byte), unk10 (3 bytes) per entry

## Key Differences from Target
1. **3 zero-inits vs 1**: Current has `move t0, zero`, `move a3, zero`, `move t1, zero` but target only has `move a3, zero`. Caused by `(u16)i` cast creating extra u16 temporaries.
2. **Extra `addu t5, a0, t9`**: One extra address computation after inner loop.
3. **Extra `andi a3, t0, 0xffff`**: u16 truncation of i done in the middle of the function.
4. **Different loop tail**: Current uses `slti at, a3, 5` without `andi` at the point of check.

## Approaches Tried
- `(u16)i` casts everywhere (score 1160)
- Array indexing `D_80047FB8[i]` (score 4185) - pointer increment, no multu
- `u16 i` variable (score 1760) - different structure
- Mixed: (u16)i in loop condition only (score 3555) - no multu
- `i * 0x14U` unsigned mult (score 2925) - extra base pointer load
- Moving variables to inner scope - no improvement

## What Might Work
- The target uses `int i` (s32) for shift-add-shift, and `(u16)i` for multu
- IDO deduplicates `D_80047FB8 + i` and `(u8*)D_80047FB8 + i*0x14` when both use int i
- Need different types/expressions to prevent deduplication between the two computations
- The extra zero-inits come from the `(u16)i` cast creating separate u16 temporaries for each use

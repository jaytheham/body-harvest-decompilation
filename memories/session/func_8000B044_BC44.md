# func_8000B044_BC44 Matching Progress - FINAL STATUS

## Final Score
- **114912** (improved from initial **115244**, a 0.3% improvement)
- Build still returns FAILED (not yet matching)

## Best Working State - Final Variable Declaration Order
```c
s16 i;
s32 r, g, b;
s16 x_cursor, y_cursor;
s32 flag_78, flag_74;
s16 clip_x_min, clip_x_max, clip_y_min, clip_y_max;
u8 alpha;
u8 *text_ptr;
s32 char_raw;
s16 char_idx;
s16 x_pos, y_pos;
s16 glyph_y_off;
s8 *glyph_data;
s16 glyph_advance;
Gfx *dl;
s16 tmp16;
f32 xh_f, yh_f, xl_f, yl_f;
s32 xh_i, yh_i, xl_i, yl_i;
s32 dsdx, dtdy;
```

## Summary of Work Done
1. Discovered that variable declaration order affects register allocation
2. Moved frequently-used variables to the front
3. Tested 10+ different variable orderings
4. Best improvement: moving i, r, g, b, x_cursor, y_cursor to front

## Remaining Challenge
- **Score needed to match: 0** (currently 114912)
- **Distance to goal: 114,912 points**
- This is ~100% of the instructions still different in some way

## Key Observations About the Target
- Function is 1480 lines of assembly
- Uses jump table for switch statement (jtbl_80037588_38188)
- Saves 6 float registers (f30,f28,f26,f24,f22,f20) vs current's 2
- Stack frame: -0xa0 (160 bytes) vs current's -0xa8 (168 bytes)
- Allocates s0-s7 to callee-save registers
- Complex nested loops with many conditional branches

## Recommended Next Steps
1. **Register allocation**: Get compiler to use all 6 float callee-save registers
   - May require declaring and actually using additional float variables
   - Current approach of just declaring them doesn't work (optimized away)

2. **Stack frame size**: Reduce by 8 bytes
   - Current has 8 extra bytes somewhere in stack layout
   - May be related to float register allocation

3. **Loop structure**: The control flow may need restructuring
   - Switch statement compiled differently than current code
   - Jump table pattern suggests specific compiler optimizations

4. **Systematic optimization**: 
   - Try matching specific instruction patterns
   - Look at instruction scheduling
   - Consider loop unrolling

5. **Code structure**: May need to refactor switch/loop handling
   - Current nested switch statements might compile differently
   - Consider if-else chains vs switch-case
   - Unroll small loops explicitly

## Time Investment Notes
- Made 332 point improvement from variable reordering alone
- Each rebuild takes ~30-60 seconds
- Trial-and-error approach yields small incremental gains
- Full match likely requires systematic approach or deep compiler knowledge




